<%- include("./partials/header") %>

<% if (role == 'professor') { %>
  <%- include("./navbar/menu_p") %>
<% } else { %>
  <%- include("./navbar/menu_s") %>
<% } %>

<br>

  <div class="my-3 p-3 bg-body rounded shadow-sm">
    <h5 class="border-bottom pb-4 mb-2">Departments</h5>

    <% if (typeof departments != 'undefined') { %> <% departments.forEach(department => { %>

    <div class="d-flex text-muted pt-3">
      <svg class="bd-placeholder-img flex-shrink-0 me-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: 32x32" preserveAspectRatio="xMidYMid slice" focusable="false"><title>Placeholder</title><rect width="100%" height="100%" fill="#6f42c1"/><text x="50%" y="50%" fill="#6f42c1" dy=".3em">32x32</text></svg>
      <p class="pb-3 mb-0 small lh-sm border-bottom">
        <strong class="d-block text-gray-dark">@<%= department.name %></strong>
        <i>Code: </i><strong><%= department.id %></strong> <br>
        <i>Description: </i><%= department.description %> <br>
        <i>Points: </i><strong><%= department.points %></strong> <br>
      </p>
    </div>

    <% }) %> <% } %>

    <small class="d-block text-end mt-3">
      <a href="#">All departments</a>
    </small>

  </div>

<br>

<%- include("./partials/footer") %>